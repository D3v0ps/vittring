Vittring — {{ total }} nya signaler ({{ digest_date }})

{% for section in sections %}
== {{ section.subscription_name }} ({{ section.items|length }} {{ "signal" if section.items|length == 1 else "signaler" }}) ==

{% for item in section.items %}
[{{ item.kind_label }}] {{ item.date_label }}
{{ item.title }}
{% if item.detail %}{{ item.detail }}{% endif %}
{% if item.source_url %}{{ item.source_url }}{% endif %}

{% endfor %}
{% endfor %}

Hantera prenumerationer: {{ manage_url }}
Avregistrera: {{ unsubscribe_url }}

{{ contact_address }}
